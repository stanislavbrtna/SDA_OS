OS features:
  - system log
  - better support for operation with lcd switched off (phone mngr, alarm)
  - Notification icon overlay:
    - When more icons are displayed than can fit the screen, hide the rest in an optional overlay.
  - Better alarm API:
    - better functions for managing of multiple alarms.
    - rewrite alarms so they can use the csv format
    - getParam(alarmId)
    - perhaps function to list all alarms for an app
    

Gui:
  - use UP and DOWN buttons to move cursor in an active text field

DB:
  - get row: array of all the row elements
  - getValue: auto return string or num, leave the typecheck on the developer

Examples:
  - ds image demo with scaling

Docs:
  - specify the native API
  - specify all the used file formats:
    - basic "ini" style file
    - basic "csv" style file
    - Application:
      - list and describe used callbacks
    - Config files:
      - svp.cfg
      - alarms.cfg
      - calib.dat
      - homescreen.cfg
    - PPM images
    - p16 images
    - autoexec.svs function


Bugfixes:
  - Unify the icon/uart and other callbacks
