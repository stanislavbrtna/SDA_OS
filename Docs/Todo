  - system log
  - clean up that app-slot/screen-slot business

Docs:
  - specify the native API
  - specify all the used file formats:
    - basic "ini" style file
    - basic "csv" style file
    - Application:
      - list and describe used callbacks
    - Config files:
      - svp.cfg
      - alarms.cfg
      - calib.dat
    - PPM images
    - p16 images
    - autoexec.svs function


Misc TODO:
  - Optimize char draw function
  - Add an iconfont or something like that


Bugfixes:
  - weird bug happens when subprocess is called and in subprocess tokenize error occurs. It crashes on "no update function found" and it takes the parent process with it.
  - weird bug with garbage collecting strings stored inside function arguments